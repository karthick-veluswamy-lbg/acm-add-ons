kind: RootSync
metadata:
  name: istio
spec:
  helm:
    values:
      gateway-objects:
        ingress:
          - name: lab1
            hosts:
            - svc1.pdev.lloydsbanking.dev
            - svc2.pdev.lloydsbanking.dev
            - svc3.pdev.lloydsbanking.dev